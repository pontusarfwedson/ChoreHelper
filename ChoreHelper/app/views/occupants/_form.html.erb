<%= form_for(occupant) do |f| %>
  <% if occupant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(occupant.errors.count, "error") %> prohibited this occupant from being saved:</h2>

      <ul>
      <% occupant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :roomnr %>
    <%= f.number_field :roomnr %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
